import 'package:flutter/material.dart';

class ChooseProfile extends StatelessWidget {
  final double amount;
  final bool considerAmount;

  const ChooseProfile({
    Key? key,
    required this.amount,
    required this.considerAmount,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
